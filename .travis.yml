language: java
jdk:
  - oraclejdk8
env:
  - SAMPLE=cxf
  - SAMPLE=spock
  - SAMPLE=xmlunit
script: "cd $SAMPLE && mvn --errors --batch-mode clean verify"
notifications:
    email:
        - martin.hofmannsobik@1und1.de
